version: '3.8'

services:
  discord-bot:
    build: .
    container_name: calendar_bot
    restart: always
    env_file:
      - .env
    # Esto asegura que el contenedor use la hora de España internamente
    environment:
      - TZ=Europe/Madrid