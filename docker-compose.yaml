version: '3.8'

services:
  discord-bot:
    build: .
    container_name: calendar_bot
    restart: always
    env_file:
      - .env
    environment:
      - TZ=Europe/Madrid
    volumes:
      - bot_data:/app/data

volumes:
  bot_data:
